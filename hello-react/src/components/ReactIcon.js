import reactIcon from '../images/react-icon.png'

const ReactIcon = (className) => {
  return (
    <div>
      <img src={reactIcon} alt="react-icon" className={className}></img>
    </div>
  )
}

export default ReactIcon